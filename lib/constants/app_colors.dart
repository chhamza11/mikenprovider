import 'package:flutter/material.dart';

/// A class to hold the custom colors for the application.
class AppColors {
  AppColors._(); // this basically makes it so you can't instantiate this class

  // App Colors
  static const Color primaryOrange = Color(0xFFF47621); // A vibrant orange for primary actions
  static const Color accentOrange = Color(0xFFFF6F03); // A slightly different orange for accents

  // Neutral Colors
  static const Color background = Color(0xFF161616);  // Main dark background color
  static const Color surface = Color(0xFF2C2C2C);     // A slightly lighter dark color for surfaces like text fields
  static const Color mediumGray = Color(0xFF525353);  // For borders, dividers
  static const Color lightGray = Color(0xFF7A7A7A);   // For hint text, icons, and less important text
   // For hint text, icons, and less important text
}